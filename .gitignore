node_modules/
dist/
.DS_Store
*.local
.env
env.txt
scripts/8x-data/*.json
scripts/8x-data/*.csv
scripts/import-*.mjs
scripts/clean-*.mjs
# Migration backups — large JSON dumps with sensitive customer data
scripts/backups/
.vercel
supabase/.temp/

# Data-ops scripts hold hardcoded service-role keys + customer-data dumps — never commit
scripts/
# Sensitive data exports (payroll, backups)
/*.csv
# Claude local config / session data
.claude/
